{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "openapi --input ../../apps/api/public/swagger.json --output ./client --name NotesClient",
    "build": "tsc",
    "clean": "rm -rf ./client && rm -rf ./turbo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "openapi-typescript-codegen": "^0.23.0",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.4"
  }
}
