{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "openapi --input ../../apps/api/public/swagger.json --output ./client --name NotesClient && tsc",
    "clean": "rm -rf ./client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "openapi-typescript-codegen": "^0.23.0",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.4"
  }
}
