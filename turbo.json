{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "generate": {
      "cache": false
    },
    "build": {
      "outputs": ["build/**", "dist/**"],
      "dependsOn": ["generate", "^build"]
    },
    "test": {
      "outputs": ["coverage/**"],
      "dependsOn": []
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    },
    "client#generate": {
      "dependsOn": ["api#generate"]
    }
  }
}
